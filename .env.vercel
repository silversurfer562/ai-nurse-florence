# AI Nurse Florence - Vercel Production Environment
# These variables should be set in Vercel Dashboard > Settings > Environment Variables
# This file serves as documentation of required Vercel environment variables

# ================================
# VERCEL DASHBOARD CONFIGURATION
# ================================
# Set these in Vercel Dashboard (vercel.com/dashboard -> project -> settings -> environment variables):

# SENSITIVE SECRETS (Production Values):
# OPENAI_API_KEY=sk-your-actual-openai-api-key-here
# API_BEARER=your-secure-production-bearer-token
# JWT_SECRET_KEY=your-super-secure-jwt-secret-key-for-production

# PRODUCTION SERVICES:
# DATABASE_URL=postgresql://user:pass@host:5432/dbname  (if using database)
# REDIS_URL=redis://user:pass@host:6379/0  (if using Redis)
# NCBI_API_KEY=your-ncbi-api-key-for-enhanced-pubmed-limits

# PRODUCTION DOMAINS:
# CORS_ORIGINS=https://your-frontend-domain.com,https://api.your-domain.com

# MONITORING (if enabled):
# GRAFANA_ADMIN_PASSWORD=your-secure-grafana-password

# ================================
# AUTO-SET BY VERCEL (No action needed)
# ================================
# VERCEL=1
# VERCEL_URL=your-app.vercel.app
# VERCEL_REGION=iad1

# ================================
# INSTRUCTIONS
# ================================
# 1. Go to vercel.com/dashboard
# 2. Select your ai-nurse-florence project  
# 3. Go to Settings > Environment Variables
# 4. Add the sensitive variables listed above with your actual values
# 5. Set them for "Production" environment
# 6. Redeploy your application

# ================================
# NON-SENSITIVE DEFAULTS (set in vercel.json or code)
# ================================
# These are handled by the application code with sensible defaults:
# USE_LIVE=true
# LOG_LEVEL=INFO  
# RATE_LIMIT_PER_MINUTE=100
# CACHE_TTL_SECONDS=3600
# FUNCTION_TIMEOUT=30
# FUNCTION_MEMORY=512
