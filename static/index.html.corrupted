<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Nurse Florence - Healthcare AI Assistant</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
                const response = await fetch(`${API_BASE}/disease/?q=${encodeURIComponent(query)}`);   <style>
        :root {
            --primary: #2563eb;
            --primary-dark: #1d4ed8;
            --secondary: #10b981;
            --white: #ffffff;
            --gray-50: #f9fafb;
            --gray-100: #f3f4f6;
            --gray-600: #4b5563;
            --gray-800: #1f2937;
            --gray-900: #111827;
            --success: #10b981;
            --error: #ef4444;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: var(--gray-800);
            background: var(--gray-50);
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }
        
        .header {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: var(--white);
            padding: 1.5rem 0;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        
        .header-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-size: 1.5rem;
            font-weight: 700;
        }
        
        .nav {
            display: flex;
            gap: 2rem;
        }
        
        .nav-link {
            color: var(--white);
            text-decoration: none;
            font-weight: 500;
        }
        
        .nav-link:hover {
            color: var(--secondary);
        }
        
        .hero {
            background: var(--white);
            padding: 4rem 0;
            text-align: center;
        }
        
        .hero h1 {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--gray-900);
            margin-bottom: 1rem;
        }
        
        .hero p {
            font-size: 1.125rem;
            color: var(--gray-600);
            margin-bottom: 2rem;
        }
        
        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            border-radius: 9999px;
            font-size: 0.875rem;
            font-weight: 500;
            margin-bottom: 2rem;
        }
        
        .status-healthy {
            background: var(--success);
            color: var(--white);
        }
        
        .status-error {
            background: var(--error);
            color: var(--white);
        }
        
        .btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            font-weight: 500;
            text-decoration: none;
            border-radius: 0.5rem;
            transition: all 0.3s;
            margin: 0 0.5rem;
        }
        
        .btn-primary {
            background: var(--primary);
            color: var(--white);
        }
        
        .btn-primary:hover {
            background: var(--primary-dark);
        }
        
        .demo {
            padding: 4rem 0;
            background: var(--gray-100);
        }
        
        .demo h2 {
            text-align: center;
            font-size: 2rem;
            margin-bottom: 3rem;
        }
        
        .demo-interface {
            background: var(--white);
            border-radius: 1rem;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            max-width: 800px;
            margin: 0 auto;
        }
        
        .demo-header {
            background: var(--gray-800);
            color: var(--white);
            padding: 1rem 1.5rem;
        }
        
        .demo-tabs {
            display: flex;
            background: var(--gray-100);
        }
        
        .demo-tab {
            flex: 1;
            padding: 1rem;
            background: none;
            border: none;
            cursor: pointer;
            font-weight: 500;
            color: var(--gray-600);
        }
        
        .demo-tab.active {
            background: var(--white);
            color: var(--primary);
        }
        
        .demo-content {
            padding: 1.5rem;
            min-height: 400px;
        }
        
        .search-form {
            display: flex;
            gap: 0.75rem;
            margin-bottom: 1.5rem;
        }
        
        .search-input {
            flex: 1;
            padding: 0.75rem 1rem;
            border: 2px solid #e5e7eb;
            border-radius: 0.5rem;
            font-size: 1rem;
        }
        
        .search-input:focus {
            outline: none;
            border-color: var(--primary);
        }
        
        .search-btn {
            padding: 0.75rem 1.5rem;
            background: var(--primary);
            color: var(--white);
            border: none;
            border-radius: 0.5rem;
            cursor: pointer;
        }
        
        .search-btn:hover {
            background: var(--primary-dark);
        }
        
        .results {
            margin-top: 1rem;
        }
        
        .result-item {
            background: var(--gray-50);
            padding: 1rem;
            border-radius: 0.5rem;
            border-left: 4px solid var(--primary);
            margin-bottom: 1rem;
        }
        
        .result-title {
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        
        .loading {
            text-align: center;
            padding: 2rem;
        }
        
        .spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 2px solid #e5e7eb;
            border-top: 2px solid var(--primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .footer {
            background: var(--gray-900);
            color: var(--white);
            padding: 2rem 0;
            text-align: center;
            margin-top: 4rem;
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <i class="fas fa-stethoscope"></i>
                    AI Nurse Florence
                </div>
                <nav class="nav">
                    <a href="#demo" class="nav-link">Try Demo</a>
                    <a href="/docs" class="nav-link">API Docs</a>
                    <a href="/health" class="nav-link">Status</a>
                </nav>
            </div>
        </div>
    </header>

    <section class="hero">
        <div class="container">
            <h1>AI Healthcare Assistant</h1>
            <p>Evidence-based medical information and clinical decision support powered by AI.</p>
            
            <div>
                <span class="status-badge" id="system-status">
                    <div class="spinner"></div>
                    Checking system...
                </span>
            </div>
            
            <div>
                <a href="#demo" class="btn btn-primary">
                    <i class="fas fa-rocket"></i>
                    Try AI Assistant
                </a>
                <a href="/docs" class="btn btn-primary">
                    <i class="fas fa-code"></i>
                    API Docs
                </a>
            </div>
        </div>
    </section>

    <section class="demo" id="demo">
        <div class="container">
            <h2>Try AI Nurse Florence</h2>
            <div class="demo-interface">
                <div class="demo-header">
                    <i class="fas fa-terminal"></i>
                    AI Healthcare Assistant
                </div>
                
                <div class="demo-tabs">
                    <button class="demo-tab active" onclick="switchDemo('disease')">Disease Info</button>
                    <button class="demo-tab" onclick="switchDemo('pubmed')">PubMed</button>
                    <button class="demo-tab" onclick="switchDemo('summarize')">Summarize</button>
                </div>
                
                <div class="demo-content" id="demo-content">
                    <!-- Content loaded by JavaScript -->
                </div>
            </div>
        </div>
    </section>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 AI Nurse Florence. Educational use only - not medical advice.</p>
        </div>
    </footer>

    <script>
        const API_BASE = '/api/v1';
        
        document.addEventListener('DOMContentLoaded', function() {
            checkSystemStatus();
            switchDemo('disease');
        });
        
        async function checkSystemStatus() {
            const statusElement = document.getElementById('system-status');
            
            try {
                console.log('Checking system status...');
                const response = await fetch('/health');
                console.log('Health response:', response.status, response.ok);
                if (response.ok) {
                    const data = await response.json();
                    console.log('Health data:', data);
                    statusElement.className = 'status-badge status-healthy';
                    statusElement.innerHTML = '<i class="fas fa-check-circle"></i> System Online';
                } else {
                    throw new Error(`Health check failed with status: ${response.status}`);
                }
            } catch (error) {
                console.error('Health check error:', error);
                statusElement.className = 'status-badge status-error';
                statusElement.innerHTML = '<i class="fas fa-times-circle"></i> System Offline';
            }
        }
        
        function switchDemo(type) {
            document.querySelectorAll('.demo-tab').forEach(tab => {
                tab.classList.remove('active');
            });
            
            const tabs = document.querySelectorAll('.demo-tab');
            const tabNames = ['disease', 'pubmed', 'summarize'];
            const index = tabNames.indexOf(type);
            if (index !== -1 && tabs[index]) {
                tabs[index].classList.add('active');
            }
            
            const content = document.getElementById('demo-content');
            content.innerHTML = getDemoContent(type);
        }
        
        function getDemoContent(type) {
            switch (type) {
                case 'disease':
                    return \`
                        <h3>Disease Information Search</h3>
                        <p>Search for comprehensive information about medical conditions.</p>
                        <div class="search-form">
                            <input type="text" class="search-input" placeholder="Enter disease (e.g., diabetes)" id="disease-input" value="diabetes">
                            <button class="search-btn" onclick="searchDisease()">Search</button>
                        </div>
                        <div id="disease-results" class="results"></div>
                    \`;
                
                case 'pubmed':
                    return \`
                        <h3>PubMed Literature Search</h3>
                        <p>Access medical research from PubMed database.</p>
                        <div class="search-form">
                            <input type="text" class="search-input" placeholder="Enter medical topic" id="pubmed-input" value="hypertension">
                            <button class="search-btn" onclick="searchPubmed()">Search</button>
                        </div>
                        <div id="pubmed-results" class="results"></div>
                    \`;
                
                case 'summarize':
                    return \`
                        <h3>Text Summarization</h3>
                        <p>Summarize medical texts and patient notes.</p>
                        <div class="search-form">
                            <textarea class="search-input" placeholder="Enter text to summarize" id="summarize-input" rows="3">Patient presents with chest pain and shortness of breath. History of hypertension.</textarea>
                            <button class="search-btn" onclick="summarizeText()">Summarize</button>
                        </div>
                        <div id="summarize-results" class="results"></div>
                    \`;
                
                default:
                    return '<p>Demo not available.</p>';
            }
        }
        
        async function searchDisease() {
            const input = document.getElementById('disease-input');
            const results = document.getElementById('disease-results');
            const query = input.value.trim();
            
            if (!query) {
                results.innerHTML = '<div class="result-item">Please enter a disease to search.</div>';
                return;
            }
            
            results.innerHTML = '<div class="loading"><div class="spinner"></div> Searching...</div>';
            
            try {
                const response = await fetch(\`\${API_BASE}/disease?q=\${encodeURIComponent(query)}\`);
                const data = await response.json();
                
                results.innerHTML = \`
                    <div class="result-item">
                        <div class="result-title">Disease Information: \${query}</div>
                        <div class="result-content">\${JSON.stringify(data, null, 2)}</div>
                    </div>
                \`;
                
            } catch (error) {
                results.innerHTML = \`<div class="result-item">Error: \${error.message}</div>\`;
            }
        }
        
        async function searchPubmed() {
            const input = document.getElementById('pubmed-input');
            const results = document.getElementById('pubmed-results');
            const query = input.value.trim();
            
            if (!query) {
                results.innerHTML = '<div class="result-item">Please enter a topic to search.</div>';
                return;
            }
            
            results.innerHTML = '<div class="loading"><div class="spinner"></div> Searching...</div>';
            
            try {
                const response = await fetch(\`\${API_BASE}/pubmed/search?q=\${encodeURIComponent(query)}&retmax=3\`);
                const data = await response.json();
                
                results.innerHTML = \`
                    <div class="result-item">
                        <div class="result-title">PubMed Results: \${query}</div>
                        <div class="result-content">\${JSON.stringify(data, null, 2)}</div>
                    </div>
                \`;
                
            } catch (error) {
                results.innerHTML = \`<div class="result-item">Error: \${error.message}</div>\`;
            }
        }
        
        async function summarizeText() {
            const input = document.getElementById('summarize-input');
            const results = document.getElementById('summarize-results');
            const text = input.value.trim();
            
            if (!text) {
                results.innerHTML = '<div class="result-item">Please enter text to summarize.</div>';
                return;
            }
            
            results.innerHTML = '<div class="loading"><div class="spinner"></div> Summarizing...</div>';
            
            try {
                const response = await fetch(\`\${API_BASE}/summarize/chat\`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ prompt: text })
                });
                
                const data = await response.json();
                
                results.innerHTML = \`
                    <div class="result-item">
                        <div class="result-title">Summary</div>
                        <div class="result-content">\${JSON.stringify(data, null, 2)}</div>
                    </div>
                \`;
                
            } catch (error) {
                results.innerHTML = \`<div class="result-item">Error: \${error.message}</div>\`;
            }
        }
    </script>
</body>
</html>
