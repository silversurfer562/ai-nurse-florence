# Local development environment variables
# Production secrets are set in Vercel Dashboard

# --- Development flags ---
# Keep external services disabled for local development
USE_LIVE=0
LOG_LEVEL=INFO

# --- Local development settings ---
# Local CORS origins (customize as needed)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# --- Rate limiting (optional for local dev) ---
RATE_LIMIT_PER_MINUTE=60

# --- Local services (optional) ---
# Uncomment if you want to run Redis locally
# REDIS_URL=redis://localhost:6379/0

# Uncomment if you want to use local SQLite database
# DATABASE_URL=sqlite+aiosqlite:///./dev.db

# --- Monitoring (optional for local dev) ---
# GRAFANA_ADMIN_USER=admin
# GRAFANA_ADMIN_PASSWORD=admin

# PRODUCTION SECRETS (set in Vercel Dashboard):
# - OPENAI_API_KEY: Your OpenAI API key
# - API_BEARER: Secure random token for API authentication
# - DATABASE_URL: Production database connection string
# - REDIS_URL: Production Redis URL (if using Redis)

# For local testing with real APIs, uncomment and set:
# OPENAI_API_KEY=your-local-test-key-here
# API_BEARER=local-test-token