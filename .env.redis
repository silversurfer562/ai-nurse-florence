# AI Nurse Florence - Redis Configuration Template
# This file is included for Docker Compose and local development

# ================================
# REDIS CACHE CONFIGURATION
# ================================

# Redis Connection (Non-Sensitive - for local development)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_MAX_CONNECTIONS=20

# Cache Settings (Non-Sensitive)
USE_REDIS=true
CACHE_TTL_SECONDS=3600
CACHE_MAX_SIZE=1000

# ================================
# PRODUCTION REDIS
# ================================
# For production, set REDIS_URL in Vercel Dashboard:
# REDIS_URL=redis://user:password@your-redis-host:6379/0
#
# For local development with Docker Compose:
# REDIS_URL=redis://redis:6379/0
#
# For local development with local Redis:
REDIS_URL=redis://localhost:6379/0

# ================================
# REDIS FEATURES (Non-Sensitive)
# ================================

# Enable Redis features
ENABLE_REDIS_CACHING=true
ENABLE_REDIS_SESSIONS=false
ENABLE_REDIS_RATE_LIMITING=false

# Redis Performance Settings
REDIS_TIMEOUT=5
REDIS_RETRY_ATTEMPTS=3
REDIS_RETRY_DELAY=1
