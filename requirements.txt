# AI Nurse Florence - Main Application Dependencies
# For local development and production deployment

# Core FastAPI Application
# fastapi pinned to a compatible range so Starlette can be upgraded for monitoring libs
# Use a modern FastAPI compatible with Starlette >=0.30
fastapi==0.115.14
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP Client & External API Integration
httpx==0.25.2

# Configuration Management
python-dotenv>=1.0.0,<1.1.0

# Form Data Processing
python-multipart>=0.0.18

# Template Engine
jinja2>=3.1.0

# Async File Operations
aiofiles>=23.2.0,<24.0.0

# Data Validation
email-validator>=2.1.0,<2.2.0

# CORS and Security
# Starlette range aligned with modern FastAPI releases used in this project.
starlette==0.46.2

# Production Server
gunicorn>=23.0.0

# Database Dependencies (enabled for production)
sqlalchemy==2.0.23
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL
aiosqlite>=0.19.0,<0.20.0  # Async SQLite support
greenlet>=3.0.0,<4.0.0  # Required for async SQLAlchemy

# Caching Dependencies (enabled for production)
redis==5.0.1

# Background jobs
celery[redis]==5.3.1

# Authentication Dependencies (enabled for production)

# Authentication
python-jose[cryptography]>=3.4.0
passlib[bcrypt]==1.7.4

# Monitoring Dependencies (enabled for production)
prometheus-client==0.19.0
python-json-logger>=2.0.7,<2.1.0
PyYAML==6.0.1
requests==2.31.0
slowapi==0.1.9

# Ensure secure versions for known vulnerable packages
python-multipart>=0.0.18
setuptools>=70.0.0

