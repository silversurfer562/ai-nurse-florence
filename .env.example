# Example .env for local development and CI
# Copy this to .env and fill sensitive values before running the app/tests.

# --- API / Auth ---
# A simple opaque token used internally by services and tests. Change this.
API_BEARER=change-me-please

# Comma-separated list of allowed CORS origins for the API. Leave empty for no CORS.
CORS_ORIGINS=http://localhost:3000,https://example.com

# --- OpenAI ---
# Optional: set if you use OpenAI integrations. Keep empty for offline/dev usage.
OPENAI_API_KEY=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

# --- Service flags ---
# Set to 1/true to enable live external services (careful in dev/CI)
USE_LIVE=0
LOG_LEVEL=INFO

# --- Rate limiting ---
RATE_LIMIT_PER_MINUTE=60

# --- Redis ---
# URL for Redis used for caching and rate limiting. Local dev default shown below.
REDIS_URL=redis://localhost:6379/0

# --- Monitoring / tools (optional) ---
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Optional: override default DB for local dev (uncomment to use)
# DATABASE_URL=sqlite+aiosqlite:///./dev.db

cp .env.example .env
# Edit .env to set OPENAI_API_KEY if needed